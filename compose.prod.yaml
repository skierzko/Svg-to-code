services:
  next-app:
    container_name: svg_to_code_next-app
    build:
      context: ./app
      dockerfile: prod.Dockerfile
      args:
        ENV_VARIABLE: ${ENV_VARIABLE}
        NEXT_PUBLIC_ENV_VARIABLE: ${NEXT_PUBLIC_ENV_VARIABLE}
    restart: always
    ports:
      - 3000:3000
    networks:
      - svg_network

networks:
  svg_network:
    external: true
